# TianyanVision

这是一个公开的数据大屏项目（RuyiBigScreen / 如意数据大屏）。

项目说明：
- 仅实现前端原型，数据使用 mock，后期可切换真实 API。
- 技术栈推荐：Vue 3 + Vite + TypeScript + ECharts（或按需选择 React）。

目录与实现要点请参见项目文档（将在仓库中补充）。

许可证：本项目采用 MIT 许可证（见 LICENSE 文件）。

如果你希望我把仓库推送到 GitHub，请提供一个远程仓库 URL，或授权我使用 `gh` / 提供个人访问令牌（PAT）。
